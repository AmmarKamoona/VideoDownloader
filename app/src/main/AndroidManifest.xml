<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Writing to public Downloads on Android 9 and below -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:name=".YtDlpApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.YtDownloader"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.YtDownloader"
            android:launchMode="singleTask">

            <!-- App launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
              Receive shared text from any app (YouTube, TikTok, Instagram,
              Twitter/X, Facebook, browsers, etc.). When a user taps Share
              and picks Legend, this filter triggers.
            -->
            <intent-filter android:label="@string/app_name">
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>

            <!--
              Direct link handling — appear as an option when users tap
              a video URL in a browser, email, or chat app.
              We list every supported host explicitly because Android's
              host="*" wildcard doesn't work for ACTION_VIEW.
            -->
            <intent-filter android:label="@string/app_name" android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" />
                <data android:scheme="http" />
                <!-- YouTube -->
                <data android:host="youtube.com" />
                <data android:host="www.youtube.com" />
                <data android:host="m.youtube.com" />
                <data android:host="music.youtube.com" />
                <data android:host="youtu.be" />
                <!-- TikTok -->
                <data android:host="tiktok.com" />
                <data android:host="www.tiktok.com" />
                <data android:host="vm.tiktok.com" />
                <data android:host="vt.tiktok.com" />
                <!-- Instagram -->
                <data android:host="instagram.com" />
                <data android:host="www.instagram.com" />
                <!-- Twitter / X -->
                <data android:host="twitter.com" />
                <data android:host="x.com" />
                <data android:host="t.co" />
                <!-- Facebook -->
                <data android:host="facebook.com" />
                <data android:host="www.facebook.com" />
                <data android:host="fb.watch" />
                <!-- Reddit -->
                <data android:host="reddit.com" />
                <data android:host="redd.it" />
                <data android:host="v.redd.it" />
                <!-- Other -->
                <data android:host="vimeo.com" />
                <data android:host="dailymotion.com" />
                <data android:host="twitch.tv" />
                <data android:host="bilibili.com" />
                <data android:host="b23.tv" />
                <data android:host="streamable.com" />
            </intent-filter>
        </activity>

        <activity
            android:name=".OverlayPermissionActivity"
            android:exported="false"
            android:theme="@style/Theme.YtDownloader" />

        <service
            android:name=".ClipboardService"
            android:exported="false"
            android:foregroundServiceType="dataSync">
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
